# Allow read queries while applying WAL changes
hot_standby = on

# Send feedback to primary to prevent vacuuming rows the replica still needs
hot_standby_feedback = on

# Performance
shared_buffers = 256MB
work_mem = 4MB