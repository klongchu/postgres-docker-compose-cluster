# PostgreSQL Client Authentication Configuration File
# TYPE  DATABASE        USER            ADDRESS                 METHOD

# Local access
local   all             all                                     trust
host    all             all             127.0.0.1/32            trust
host    all             all             ::1/128                 trust

# Physical replication — restrict ADDRESS to replica IPs in production.
# Only roles with the REPLICATION attribute can use this connection type.
host    replication     all             0.0.0.0/0               scram-sha-256

# Application connections — restrict ADDRESS in production.
host    all             all             0.0.0.0/0               scram-sha-256
